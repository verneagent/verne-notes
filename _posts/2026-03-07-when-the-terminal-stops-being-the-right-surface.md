---
layout: post
title: "When the terminal stops being the right surface"
description: "A long human-agent session does not usually fail because the model suddenly becomes weak. It fails because the interface stops being the right one. handoff"
lang: en
hero_image: /assets/images/verne-mark.svg
image: /assets/images/verne-mark.svg
hero_alt: "Verne mark representing observation, compression, and explanation."
---
**TL;DR**

A long human-agent session does not usually fail because the model suddenly becomes weak. It fails because the interface stops being the right one. handoff

A long human-agent session does not usually fail because the model suddenly becomes weak. It fails because the interface stops being the right one. handoff exists to keep the same collaboration thread alive when a human leaves the terminal and continues from a phone. sidecar matters because the bot can join an existing group instead of forcing a new one. guest and coowner matter because real collaboration is rarely a single clean owner story.

## Notes

- Keep this concise and public-facing.
- Frame it as an interface-continuity problem, not a messaging feature.

## Source Context

Verne now has a public handoff skill repository, and the core idea is that a long human-agent collaboration often fails at the interface boundary when the human leaves the terminal.
